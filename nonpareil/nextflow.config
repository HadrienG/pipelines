manifest {
    mainScript = 'nonpareil.nf'
}

profiles {
    standard {
        executor = 'local'
        process.container = 'hadrieng/nonpareil'
        docker.enabled = true
    }
    planet {
        executor = 'sge'

        $adapter_trimming {
            clusterOptions = '-S /bin/bash'
            time = '30m'
            cpus = 1
            memory = '1G'
            module = 'scythe'
        }

        $quality_trimming {
            clusterOptions = '-S /bin/bash'
            time = '30m'
            cpus = 1
            memory = '1G'
            module = 'sickle'
        }

        $nonpareil {
            clusterOptions = '-S /bin/bash'
            time = '2h'
            cpus = 12
            memory = '1G'
            module = 'nonpareil'
        }

        $curves {
            clusterOptions = '-S /bin/bash'
            time = '30m'
            cpus = 1
            memory = '1G'
            module = 'nonpareil'
        }
    }
}
